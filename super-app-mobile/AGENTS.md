# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

# Multi-Device Workflow
The user develops this project across two locations (home and company). At the beginning of a session, or if the user mentions working on something previously that isn't appearing locally, ALWAYS check `git status` and proactively run `git pull` to fetch their latest work from GitHub before making assumptions or trying to rewrite code.

# Shopping Module Design Theme
- The shopping module (`src/app/shopping/*`) MUST ALWAYS use **Green (`#00B14F`)** as its primary dominant color palette across all pages (home, product details, cart, checkout, shop, brand, live, shorts, seller center, admin). Never use orange/red for shopping elements.

